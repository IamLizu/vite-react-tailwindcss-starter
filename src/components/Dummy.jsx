import React from "react";

export default function Dummy() {
    return (
        <div>
            {/* I am a unused component, created just to push this directory to git. */}
        </div>
    );
}
